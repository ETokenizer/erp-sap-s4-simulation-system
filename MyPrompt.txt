我现在要做一个「ERP SAP S4 HANA上线模拟演练系统」，把SAP S4 HANA上线过程做成策略游戏Simuliation Game  SLG形式，给领导和新员工快速看懂整个上线流程。

请你按照下面的需求，帮我完整设计并实现这个系统，包括前端界面、后端逻辑、数据结构。

系统定位：
1. 面向企业领导、项目组成员，用来模拟ERP从准备到上线的全过程。
2. 全程游戏化、关卡化、可视化，不要复杂技术术语。
3. 自动产出ERP SAP S4 HANA上线需要的所有文档，加少量示例数据，不需要用户手动写，让用户理解文档作用以及模板格式，请提供一个界面，供用户输入模拟项目的必要信息，背景等，再请参考该个项目初始化时提供的信息，再用于，辅助你生成提示词， 并调用AI功能去生成相应的文档交付物

核心功能要求：
1. 把ERP上线分成6个游戏章节，体现「ERP SAP S4 HANA上线」的关键阶段，
   可按照SAP公司的ACTIVATE实施方法论框架来定以下几个阶段（章节）
   包含以下内容：
   .项目准备与计划，
   .需求收集、业务蓝图设计，AS-IS, Fit-Gap Analysis
   .系统配置、开发与流程搭建, 单元测试、集成测试
   .用户验收测试、用户培训、上线准备
   .正式切换与上线护航....等等

2. 每个章节包含：
   - 关键任务列表
   - 关卡进度条 %，
   - 风险提示，问题（可以随机模拟出现一些问题，识别出一些风险，并在项目过程“模拟”解决）
   - 产出物文档（可预览、可导出，比如项目计划，比如蓝图设计文档）

3. 必须内置使用AI自动生成文档的功能，按包括但不限于：
   - 项目实施计划
   - 业务蓝图文档
   - 主数据清理清单
   - 流程设计文档
   - 测试用例
   - 上线切换方案
   - 应急预案
     等一系列文档，
   用户完成对应关卡，系统自动生成这些文档的内容结构，所有文档，加少量示例数据。并且可以导出到目录

4. 界面风格要求：
   - 简洁、专业、企业级风格
   - 左边是任务导航，中间是内容区，右边是当前产出文档
   -下面主要内容的底部显示一个甘特图
   - 用步骤条、卡片、进度条展示类游戏化流程
   - 适合领导观看，清晰、大气、不花哨

5. 技术栈：
   前端使用HTML + CSS + JavaScript + Bootstrap
   可以用本地JSON模拟数据，不需要真实数据库

请你直接给出完整可运行的代码结构，分文件给出，我可以直接在浏览器里运行。

前端页面：

请基于上面的「ERP SAP S4 HANA上线模拟演练系统，帮我实现完整的前端页面。

页面包含：
1. 顶部：系统名称、进度总览
2. 左侧：各大章节导航，可点击切换
3. 中间：当前章节的任务卡片，每个任务有状态：未开始 / 进行中 / 已完成，
4. 右侧：当前关卡会自动生成的文档列表，可点击预览

交互要求：
1. 点击任务，弹出简单的操作界面，完成后标记为已完成
2. 完成任务后，右侧对应的文档自动变为“已生成”
3. 文档预览用模态框展示内容，样式正式，像真实项目文档
4、前一阶段（比如设计阶段）未完成，不得进入后续阶段， 除非允许并行的任务

UI要求：
- 使用Bootstrap实现
- 企业蓝色风格
- 适配电脑端浏览
- 文字清晰，领导能一眼看懂

请直接给我完整代码，按index.html、style.css、script.js、data.json等分开。

后端功能：
请为我的「ERP SAP S4 HANA上线模拟演练系统」实现JS逻辑，实现“自动生成ERP文档”的核心功能。

要求：
1. 用data.json里的章节、任务信息。
2. 用户每完成一个任务，JS自动拼接生成对应文档内容，
3. 文档内容是真实ERP上线会用到的标准格式，包括：
   - 文档编号
   - 版本
   - 编制人/日期
   - 内容结构（真实、可用、直接能交给客户，稍作修改即可使用的那种）

需要自动生成的文档如下：
1. 项目实施计划
2. 业务蓝图设计说明书
3. 主数据准备与清洗方案
4. 系统配置清单
5. 测试方案与测试用例
6. 用户操作手册大纲
7. 上线切换执行方案
8. 上线应急预案
9.  各类项目管理用计划、文档，比如项目章程，Project Schedule, Change Request, Issues Log, Risk Log, Action List

请实现：
- 生成文档内容的JS函数
- 点击“预览文档”可以展示
- 界面简洁正式
- 可调用AI功能动态生成，不要写死，如有必要可基于不同的项目背景、用户提供的关键提示词重新生成（请另一可配置阿里云的AI 的页面，让用户输入BaseURL,Token等，且提供界面给用户输入，并生成结构化的提示词给AI

代码直接给完整可运行版本，先做一个网页版，以后考虑手机App, 考虑商业化方向

请先做一个完整的开发计划，并严格按计划执行，如果你明白了，
请开始。第一件事情是在“C:\Users\lh_js\OneDrive\Documents\agent101toolkit\claude-code\tools\windows"目录下，建一个新的工作目录
起一个和项目相关的英文的目录名称，并使用这个目录作为本项目的工作目录。然后在其中放上我们的第一个文档，即：本项目的开发计划。
